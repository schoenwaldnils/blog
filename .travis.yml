language: node_js
node_js:
  - "5"
before_script:
  - \command \which 'curl' || echo 'NO CURL'
  - rvm install ruby-2.3.0
  - rvm use 2.3.0
  - bundle install --jobs=3 --retry=3 --deployment
  - chmod +x ./deploy.sh
script: bash ./deploy.sh
branches:
  only:
    - master
env:
  global:
    ENCRYPTION_LABEL: "1d97a522c7ba"
    COMMIT_AUTHOR_EMAIL: "bot@schoenwald.media"
